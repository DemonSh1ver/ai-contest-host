# 第 3 天实践报告：主路径数据对象与调用约定

## 一、基本信息

| 栏目 | 内容 |
| ---- | ----------------------- |
| 课题   | 算法竞赛 AI 智能主持人           |
| 成员   | 姚子秋（独立完成，1 人）           |

## 二、今日目标

第 1 天已经确定项目的最小主路径，第 2 天完成了模块划分和技术选型。今天继续完成 Day 2 遗留的 `parser → roll → script` 接口核对，重点不是继续增加外围功能，而是把主路径上的数据对象、模块接口、成功与失败情况以及后续实现边界固定下来，确保后续开发可以按同一套约定继续进行。

当前主路径为：

```text
排行榜 JSON
→ parser
→ Leaderboard
→ roll
→ RollStep
→ script
→ 中文主持词
→ tts
→ 播放
```

在梳理过程中发现，如果滚榜模块只按照最终排行榜从最后一名倒序到第一名，虽然能够满足第 1 天确定的 MVP，但实际演示时更接近"倒序念榜"，没有充分体现算法竞赛滚榜过程中"成绩解冻、排名变化和现场悬念"的特点。

因此，今天在不破坏原有 MVP 主路径的前提下，进一步记录了增强版滚榜的后续方向（数据与接口的约定方向）。后续可以在此基础上实现简化 ICPC 风格的滚榜过程，并根据排名变化生成不同情境的主持词。

需要明确的是：真实滚榜属于 MVP 之外的特色增强，不改变第 1 天确定的验收边界与最小功能集。

## 三、主路径上的数据或对象

本节只列支撑主路径的关键实体，不展开用户、权限、数据库等与当前课题无直接关系的设计。

| 数据对象          | 主要字段                                                        | 用途                                     |
| ------------- | ----------------------------------------------------------- | -------------------------------------- |
| `Team`        | `rank`、`name`、`solved`、`penalty`                            | 表示一支参赛队伍，是当前榜单和主持词逐队播报的基本单位            |
| `Leaderboard` | `contest_name`、`round_no`、`teams`                           | 表示一场比赛的排行榜，是 `parser` 的主要产出            |
| `RollStep`    | `step`、`revealed_team`、`ranking`                              | 表示一次滚榜揭晓，是 `roll` 和 `script` 之间的主要交接对象 |
| 主持词文本         | `str`                                                       | `script` 生成的中文文本，逐句组织，最终交给 TTS 播放      |

以上对象与当前代码中的 dataclass 字段完全一致，均为已实现、支撑 MVP 主路径的关键实体。后续真实滚榜增强所需的 `Submission` 数据与 `RollStep` 扩展字段属于 MVP 之外的特色功能，不在本节主路径范围内，其约定方向见第五节。

## 四、调用约定

当前主路径采用单向数据流，上一步的输出作为下一步输入：

```text
JSON
  ↓
parse_leaderboard_file(path)
  ↓
Leaderboard
  ↓
reveal_order(teams)
  ↓
List[RollStep]
  ↓
generate_roll_script(lb, steps)
  ↓
中文主持词 str
  ↓
init_engine() / speak(engine, text)
  ↓
TTS 播放
```

根据当前代码，主要接口约定如下：

| 函数                                      | 输入                             | 输出               | 作用                    |
| --------------------------------------- | ------------------------------ | ---------------- | --------------------- |
| `parse_leaderboard_file(path)`          | JSON 文件路径                      | `Leaderboard`    | 读取并解析排行榜              |
| `reveal_order(teams)`                   | `List[Team]`                   | `List[RollStep]` | 按当前排行榜从最后一名到第一名生成揭晓步骤 |
| `generate_script(lb)`                   | `Leaderboard`                  | 中文 `str`         | 普通模式生成主持词             |
| `generate_roll_script(lb, steps)`       | `Leaderboard`、`List[RollStep]` | 中文 `str`         | 根据滚榜步骤生成主持词           |
| `init_engine()` / `speak(engine, text)` | 无 / 中文 `str`                   | TTS 引擎 / 播放      | 完成语音播报                |

### 成功一例

输入 `data/sample_leaderboard.json`，榜单包含 4 支队伍。

程序执行：

```text
parse_leaderboard_file
→ 得到 4 支队伍的 Leaderboard
→ reveal_order
→ 得到 4 个 RollStep
→ 揭晓顺序：摸鱼队 → 红名大神 → 不会就选C → AC 冲鸭
→ generate_roll_script
→ 生成中文滚榜主持词
```

输出中能够出现类似：

> "第四名，摸鱼队，解出三题。"

以及：

> "第一名，AC 冲鸭，解出五题。"

说明现有 MVP 的"解析 → 滚榜 → 主持词"文本链路已经打通。

### 失败一例

输入文件 `test_invalid.json`：

```json
{
  "teams": 123
}
```

实际执行命令：

```bash
python main.py test_invalid.json --roll
```

`teams` 类型错误（是数字 123，不是要求的列表），程序在 `parse_leaderboard_file` 阶段校验并抛出真实异常：

```text
ValueError: 排行榜 JSON 的 teams 必须是列表，实际为 int
```

完整真实终端输出（含 traceback）见 `reports/day03_failure_example.txt`。解析失败后程序在解析阶段终止，不进入 `roll`、`script` 和 `tts`，避免错误数据继续传播。

其余异常情况（非法 JSON、顶层非对象、队伍超上限、`teams` 缺失、队伍字段缺失等）的真实执行结果统一记录在 `reports/day03_parser_failure_log.txt`，正文不再逐一展开。

## 五、临时假实现与边界

### `roll` 的当前实现与增强方向

当前 `roll.reveal_order` 只按已有最终排名倒序揭晓（从最后一名到第一名），满足第 1 天确定的 MVP，但尚未模拟真正的滚榜过程（封榜、隐藏提交、提交解冻、成绩重算、排名变化）。

真实滚榜增强方向（属于 MVP 之外的特色增强，不代表 Day 3 已经完成）：

- 第 4 天开始引入 `Submission` 数据；
- 第 4 天开始实现简化 ICPC 风格的成绩计算和排名变化；
- 后续扩展 `RollStep`，记录旧排名、新排名、成绩变化。

这些增强不改变第 1 天"最终可交付的最小功能集"，只是用于提升滚榜真实性和现场表现。

### `script` 的当前实现

主持词目前采用规则模板生成，不依赖在线大模型。

该方案属于正式 MVP 实现，不是等待替换的假实现。

后续增强方向不是简单接入 API，而是根据滚榜事件选择不同主持情境，例如：

```text
普通揭晓
排名不变
排名上升
反超
大幅跃升
进入前三
进入前二
冠军揭晓
```

使主持词能够随着比赛状态变化，而不是从头到尾播放一份固定台本。

## 六、项目备忘要点

### 技术栈

```text
Python 3
标准库 json
pyttsx3 + Windows SAPI5
unittest
```

### 约定索引

本报告各类约定的位置一览，便于后续快速查阅：

| 约定内容 | 所在章节 |
| --- | --- |
| 数据对象与字段 | 第三节 |
| 调用约定（成功一例 / 失败一例） | 第四节 |
| 临时假实现与边界 | 第五节 |
| 滚榜规则 | 第六节 |
| 事件类型 | 第六节 |
| 待决问题 | 第六节 |

### 当前主路径

```text
JSON
→ parser
→ Leaderboard
→ roll
→ RollStep
→ script
→ TTS
```

### 后续增强路径

```text
滚榜状态变化
→ 事件信息（事件约定）
→ 情境识别
→ 主持词选择
→ TTS
```

### 滚榜规则初步约定

后续采用简化 ICPC 风格规则：

```text
Solved：已通过题目数量

Penalty：
每道 AC 题的提交时间
+
20 × 该题此前的 WA 次数
```

排名按照：

```text
Solved 降序
Penalty 升序
```

进行。

### 事件类型初步约定

以下为"后续计划识别的事件类型"，Day 3 仅作记录，尚未实现事件识别，也不存在 `event.py` 或 `event.detect()`；真正的事件识别放到后续开发。

```text
NORMAL_REVEAL   普通揭晓
RANK_UP         排名上升
OVERTAKE        发生反超
BIG_JUMP        大幅跃升
TOP3            进入前三
TOP2            进入前二
CHAMPION        冠军揭晓
```

后续如果一个步骤同时满足多个事件，将按照事件优先级选择主要主持情境，避免一次播报堆叠过多信息。

### 待决问题

1. 多个事件同时发生时如何确定主要事件；
2. 主持词模板如何避免连续重复；
3. 不同赛事阶段是否需要不同 TTS 语速和停顿；
4. `RollStep` 扩展字段如何与当前代码兼容。

## 七、今日完成与自检

### 今日完成

1. 明确了主路径上的数据对象：`Team`、`Leaderboard`、`RollStep`、主持词文本。
2. 固定了当前 `parser → roll → script → tts` 的模块调用约定。
3. 明确了成功案例与失败案例，并以当前代码能够识别的情况作为依据。
4. 验证了当前 MVP 链路 `parser → roll → script → tts` 可正常运行。

> 说明：`Submission` 数据、真实滚榜、事件识别均属于 MVP 之外的后续增强，第 3 天只做约定记录、未实现，不计入今日完成。

### 自检

| 检查项                | 结果                   |
| ------------------ | -------------------- |
| 约定是否齐备           | 是，数据对象、接口约定、成功/失败案例均已写明 |
| 假实现边界是否都已标明      | 是，`roll` 当前仅按 rank 倒序、真实滚榜与事件识别均标注为后续增强 |

## 八、问题、计划与 AI 沟通

### 不成功的沟通

第一次让 AI 直接设计完整的智能主持人系统时，容易得到包含 Web、数据库、账号和在线大模型等内容的大型方案，其中很多功能与本项目的核心滚榜流程没有直接关系。

另外，让助手直接"列出所有数据对象与字段"时，助手给出含用户、权限、日志、配置等十余个实体的完整库设计，大部分与主路径无关。处理方式：把提示收窄为"只列支撑主路径的关键对象，其余删掉"，再把字段表从十余项裁剪到 4~5 个关键对象，才得到可用结果。

此外，在前期方案讨论中曾把"封榜解冻、罚时重算"直接定义成后续必做功能，这与第 1 天确定的 MVP 边界存在冲突。处理方式：把它降级为"MVP 之外的特色增强"，MVP 路径只保留倒序揭晓。

### 处理方式

重新将项目范围分成两层：

```text
MVP：
JSON → 排名 → 主持词 → TTS

特色增强：
比赛数据 → 滚榜状态变化 → 事件识别 → 情境化主持 → TTS
```

这样既保证原来的 MVP 能按计划交付，又能够在后续实践中围绕"真正的滚榜主持"增加项目特色。

### 比较有效的沟通

要求 AI：

1. 只围绕主路径定义数据对象；
2. 调用约定只写主路径最关键一步，且只写"成功一例 + 失败一例"，并给"输入要点 + 输出要点"的固定格式——这样一次就能得到可用约定，避免助手堆砌一整页异常清单；
3. 所有"已完成"必须以实际代码和运行结果为准；
4. 对增强功能明确区分"当前实现"和"后续计划"，不能提前写成已经完成；
5. 不为了体现 AI 而强行引入在线 API。

采用这种方式后，项目的基础 MVP 和后续特色增强可以保持连续。

### 明日安排

第 4 天进入"工程起步"，聚焦环境复现与代码审查，不实现真实滚榜：

1. 完善 README 启动说明，让第二人能按文字独立跑通；
2. 固定虚拟环境与依赖安装方式；
3. 通读现有代码并形成审查纪要；
4. 检查 `.gitignore`；
5. 完成至少一次真实 Git 提交。

真实滚榜增强（`Submission`、ICPC 罚时、封榜解冻、排名变化）推迟到第 5 天开始，为第 5 天的完整主路径演示做准备。
